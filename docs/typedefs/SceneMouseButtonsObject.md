# SceneMouseButtonObjects

Interface that represents an object existing within a ``Scene`` class that
will automatically be modified by the user mouse behaviour.

- - -

## Interface 

```ts
interface SceneMouseButtonsObject {
	right: boolean;
	middle: boolean;
	left: boolean;
}
```